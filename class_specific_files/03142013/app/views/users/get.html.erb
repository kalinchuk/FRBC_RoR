<table border="1">
	<% @users.each do |user| %>
		<tr>
			<td><%= link_to(user[:name], {:action => 'show', :name => user[:name]}) %></td>
			<td><%= link_to(user[:email], "mailto:#{user[:email]}") %></td>
		</tr>
	<% end %>
</table>

<br><br>

<%= Time.now %>